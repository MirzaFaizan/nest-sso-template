export const AUTH_ACCESS_TOKEN = 'ic-access-token';
export const AUTH_REFRESH_TOKEN = 'ic-refresh-token';

export enum Environments {
  PRODUCTION = 'production',
  DEVELOPMENT = 'development',
  TEST = 'test',
}

export enum SupportedApps {
  PUBLIC = 'public',
  ADMIN = 'admin',
  DASHBOARD = 'dashboard',
}
